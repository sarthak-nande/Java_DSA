package mathematics;

public class LcmOfTwoNumbers {
	
	public static int naiveMethod(int a, int b) {
		int res = Math.max(a, b);
		
		while(res>0) {
			if(res%a==0 && res%b == 0) {
				break;
			}
			res++;
		}
		
		return res;
	}

}
